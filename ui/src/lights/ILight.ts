
export interface ILightState {
    name: string, 
    unique_id: string, 
    brightness: number
}

export interface ILightConfiguration {
    name: string;
    brightness: boolean;
    unique_id: string;
}