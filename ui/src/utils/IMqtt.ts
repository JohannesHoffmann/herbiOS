export enum Topic {
    namespace = "herbiOs",
    set = "set",
    state = "state",
    config = "config",
}


export enum SubTopic {
    light = "lights",
    switch = "switches",
    fan = "fans",
    climate = "climates",
    geoPosition = "geoPosition",
    sensor = "sensors",
}